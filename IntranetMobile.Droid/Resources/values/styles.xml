<?xml version="1.0" encoding="utf-8"?>

<resources>
  <style name="BSTheme" parent="BSTheme.Base">
  </style>
  <!-- Base theme applied no matter what API -->
  <style name="BSTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="windowNoTitle">true</item>
    <item name="android:windowBackground">@color/colorPrimaryLight</item>
    <!-- Set theme colors from http://www.google.com/design/spec/style/color.html#color-color-palette-->
    <!-- colorPrimary is used for the default action bar background -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <!-- colorPrimaryDark is used for the status bar -->
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <!-- colorAccent is used as the default value for colorControlActivated
         which is used to tint widgets -->
    <item name="colorAccent">@color/colorAccent</item>
    <!-- You can also set colorControlNormal, colorControlActivated
         colorControlHighlight and colorSwitchThumbNormal. -->
    <item name="colorButtonNormal">@color/colorButtonNormal</item>
    <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
  </style>
  <style name="bsButton" parent="Base.Widget.AppCompat.Button.Colored">
    <item name="android:layout_width">0dp</item>
    <item name="android:layout_height">0dp</item>
    <item name="android:textSize">@dimen/text_size_normal</item>
    <item name="android:minHeight">0dp</item>
    <item name="android:minWidth">0dp</item>
    <item name="android:gravity">center</item>
    <item name="android:includeFontPadding">false</item>
    <item name="android:textColor">#727272</item>
    <item name="layout_rowWeight">1</item>
    <item name="layout_columnWeight">1</item>
    <item name="layout_gravity">fill</item>
  </style>
</resources>